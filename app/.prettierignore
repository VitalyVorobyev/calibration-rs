dist
node_modules
src-tauri/target
src-tauri/gen
tsconfig.tsbuildinfo
bun.lock
playwright-report
test-results

# Rust-emitted JSON Schemas (B-QUAL2): byte-for-byte owned by the
# `emit_schemas` binary and drift-checked against it, so prettier must not
# reformat them. The generated *.ts under src/types/generated IS formatted
# (the generate:types script runs prettier over it) and stays checked.
schemas-generated

# Prettier's markdown formatter mis-reformats blockquote continuation
# lines that contain a code span split across the wrap (observed
# corrupting README.md's Tauri-context callout) — out of scope for this
# lint/format pass, which targets the TS/TSX + config-file surface.
*.md
