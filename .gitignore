target
stereo
Cargo.lock
DS8
*.pyc
data/stereo_charuco
REVIEW.md
# `app/src-tauri/` is a binary (the diagnose viewer) — the rest of the
# workspace is libraries. Binaries pin their dep tree via Cargo.lock, so
# override the global ignore for that one path.
!/app/src-tauri/Cargo.lock