# Quickstart

This chapter will provide copy-paste snippets to get moving quickly.

- Installing dependencies: Rust toolchain, `cargo`, optional `mdbook` for docs.
- Adding `calib` from Git to a project and enabling features.
- Running the planar intrinsics pipeline with synthetic data.
- Building and running the CLI (`calib-cli`) on a JSON dataset.
- Generating documentation: `cargo doc --workspace --no-deps`.

> TODO: add minimal runnable examples and downloadable sample JSON inputs.
