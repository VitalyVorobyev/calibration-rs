//! Mathematical utilities — re-exported from `vision-geometry`.
//!
//! This module delegates to [`vision_geometry::math`] for backward
//! compatibility. New code should depend on `vision-geometry` directly.

pub use vision_geometry::math::*;
